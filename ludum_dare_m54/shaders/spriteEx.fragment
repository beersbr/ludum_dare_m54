#version 330 core

#extension GL_EXT_texture_array : enable

in vec2 UV;
in float texIndex;

uniform sampler2DArray sampler;

out vec4 FinalColor;

void main()
{
	FinalColor = texture2DArray(sampler, vec3(UV, texIndex));
}