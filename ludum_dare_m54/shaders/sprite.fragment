#version 330 core

in vec2 UV;

uniform sampler2D textureSampler;
uniform sampler2D normalSampler;

// uniform vec3 light0;
// uniform vec3 ligth1;
// uniform vec3 light2;
// uniform vec3 light3;
// uniform vec3 light4;

out vec4 FinalColor;

void main()
{
	FinalColor = texture(textureSampler, UV); // vec4(1.0, 0.0, 1.0, 1.0);
}